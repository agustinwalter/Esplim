import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  shape: {
    borderRadius: 20
  },
  palette: {
    primary: {
      light: '#2196f3',
      main: '#2196f3',
      dark: '#1769aa'
    }
  }
})

export default theme